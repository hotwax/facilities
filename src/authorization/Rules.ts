export default {
  "COMMON_ADMIN": "COMMON_ADMIN"  // TODO: update the permission check logic inside action, kept key as COMMON_ADMIN as the permission checking logic uses keys to check against the env value
} as any