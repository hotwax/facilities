import { api, client, getConfig, getTelecomCode, getUserFacilities, hasError, initialise, logout, resetConfig, setUserLocale, updateInstanceUrl, updateToken } from '@hotwax/oms-api'

export {
  api,
  client,
  getConfig,
  getTelecomCode,
  getUserFacilities,
  hasError,
  initialise,
  logout,
  resetConfig,
  setUserLocale,
  updateInstanceUrl,
  updateToken
}